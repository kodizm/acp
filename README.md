# kodizm-acp

Custom ACP (Agent Client Protocol) server for the Kodizm runtime. Bridges Claude Code, Codex, and opencode CLIs through a single Kodizm-flavored ACP surface.

Replaces the upstream `claude-agent-acp` and `codex-acp` adapters with one maintained codebase. Status: **early development**.
